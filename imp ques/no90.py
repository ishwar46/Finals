# 90.	Write a program that prompts the user to input an integer and 
# then outputs the number with the digits reversed. For example, 
# if the input is 12345, the output should be 54321.

integer= int(input("Enter a integer: "))
print("The reversed integer are", str(integer)[::-1])


